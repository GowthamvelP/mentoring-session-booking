source "https://rubygems.org"

# Bundle edge Rails instead: gem "rails", github: "rails/rails", branch: "main"
gem "rails", "~> 8.1.3", ">= 8.1.3.1"
# Use postgresql as the database for Active Record
gem "pg", "~> 1.1"
# Use the Puma web server [https://github.com/puma/puma]
gem "puma", ">= 5.0"
# Build JSON APIs with ease [https://github.com/rails/jbuilder]
# gem "jbuilder"

# Use Active Model has_secure_password [https://guides.rubyonrails.org/active_model_basics.html#securepassword]
gem "bcrypt", "~> 3.1.7"

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem "tzinfo-data", platforms: %i[ windows jruby ]


# Reduces boot times through caching; required in config/boot.rb
gem "bootsnap", require: false

# Deploy this application anywhere as a Docker container [https://kamal-deploy.org]
# Not used in development (using docker-compose instead) — available for production Kamal deployment
gem "kamal", require: false

# HTTP asset caching/compression and X-Sendfile acceleration for Puma [https://github.com/basecamp/thruster/]
# Not active in development — in production, acts as reverse proxy in front of Puma
# (handles SSL termination, asset caching, X-Sendfile without nginx)
gem "thruster", require: false






# === Application gems ===
gem "sidekiq", "~> 7.0"
gem "blueprinter"
gem "rack-attack"
gem "acts_as_tenant"
gem "strong_migrations"
gem "lograge"
gem "request_store"
gem "pagy"
gem "pg_search", "~> 2.3"
gem "rack-cors"
gem "redis", "~> 5.0"
gem "dotenv-rails"
gem "connection_pool", "~> 2.4"

group :development, :test do
  gem "debug", platforms: %i[mri windows], require: "debug/prelude"
  gem "brakeman", require: false
  gem "bundler-audit", require: false
  gem "rubocop-rails-omakase", require: false
  gem "rspec-rails"
  gem "factory_bot_rails"
  gem "shoulda-matchers"
  gem "rantly"
  gem "database_cleaner-active_record"
  gem "letter_opener_web"
end

group :test do
  gem "simplecov", require: false
end
